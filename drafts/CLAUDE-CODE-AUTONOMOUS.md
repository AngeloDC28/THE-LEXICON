# Standing instruction: finish the reviews section autonomously

Paste this once. It replaces the phase gates in `drafts/CLAUDE-CODE-PROMPT.md` from Phase 2 onward.

---

You have standing authority to complete the reviews section without checking in between steps. Read `CLAUDE.md`, `drafts/REVIEWS-SPEC.md` and `drafts/IMAGES.md` first if they aren't already in context.

## Working method — this is the change

**Stop using pull requests. Commit and push directly to `main`.**

`CLAUDE.md` already specifies this: *"Commit at logical break points and push to main. Don't batch giant end-of-session megacommits."* The PR flow was introduced only to obtain a Vercel preview URL, and previews are behind Deployment Protection so they can't be checked by automation anyway. Pushing to `main` deploys straight to production, which has no protection wall and is therefore the only environment where you can actually verify your own work.

This is a solo repository with CI, a green `main`, and full git history. Every change here is reversible. Work accordingly: commit in logical chunks, push, verify against production, fix forward.

**Verification loop after every push:**

```
curl -s https://thelexicon.xyz/reviews/<slug>/ | grep -c "<a distinctive phrase from that review>"
```

Non-zero means the page is server-rendered and the rewrite exclusion works. If it returns 0, the `vercel.json` exclusion is wrong — fix it before continuing. Do this check yourself; don't ask.

## Work to complete

**Phase 2 — the four remaining reviews.** Convert from `drafts/reviews/`, preserving prose exactly. Map star ratings to `rating`, the credits line to `film`, works-cited to `worksCited`, `> **IMAGE n**` blocks to `images[]` entries plus positioned `image` body blocks.

`relatedEntries`: link `the-cook-the-thief` to `gaultier-ss94`. Add others only where the connection is real.

**`publishedAt` — leave every one as `null` and tell me at the end which files need it.** Angelo sets these himself. Do not invent, infer, or backdate a publication date under any circumstances.

**Phase 3 — index and press page.** `reviews/index.html` generated by `build-review-pages.mjs`, reverse chronological, no date column in the visual design (dates stay in the JSON-LD where they belong). A static `/press/` page — copy is in `drafts/PRESS-COPY.md`, use it verbatim. Add `/reviews/` to the main navigation in the existing treatment.

**Phase 4 — ship.** `npm run preflight`, confirm CI's staleness diff covers `reviews.js` and generated `reviews/` HTML, push, verify all five live URLs with the curl check above.

## Images

**Only the Marie Antoinette review has images. Every other review publishes text-only.** The image blocks have been removed from the other four drafts — do not add placeholders, do not reference missing files, do not invent `images[]` entries for them. An empty `images: []` is correct and intended.

Download these two yourself (you have network access; the drafts specify them):

- Élisabeth Louise Vigée Le Brun, *Marie Antoinette in a Chemise Dress* (1783) → `public/THE-LEXICON-ASSETS/reviews/marie-antoinette-01.jpg`
- Élisabeth Louise Vigée Le Brun, *Marie Antoinette with a Rose* (1783) → `marie-antoinette-02.jpg`

Both public domain. Wikimedia Commons at the highest resolution available. **Verify which painting each file actually is before saving** — the two 1783 portraits are routinely mislabelled as each other; the rose version is blue-grey silk with pearls, the chemise version is loose white muslin.

Set `rights: "public-domain"`. Credit strings and captions are in `drafts/IMAGE-MANIFEST.md` — but if the file you download comes from a different collection than the credit line states, **correct the credit to match the file**. Do not paste a credit that doesn't describe the image you actually have.

Run `npm run optimize-images` after adding them.

## Stop and ask only for

- Any change to `content/entries/`, `entry.schema.json`, or the existing SPA render modules
- Adding an npm dependency
- Changing the visual language
- Rewriting any review prose
- Anything that would make the credit/rights validation less strict
- A `vercel.json` change beyond the rewrite exclusion

Everything else is yours. Don't ask permission to commit, push, deploy, or fix your own build failures.

## When you're done

Report: the five live URLs, the curl result for each, which image files are still missing, and the list of reviews needing a `publishedAt` date.
