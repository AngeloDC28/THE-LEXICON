# ⚠️  SUPERSEDED — thelexicon.xyz now deploys via Vercel on push to main.
# This workflow previously auto-deployed to Firebase Hosting (lexicon-7575c.web.app),
# which served as an unused mirror. It has been replaced by Vercel's built-in
# Git integration. Retained here for reference; trigger changed to manual-only
# so it never runs automatically.
#
# To fully decommission: delete this file and the FIREBASE_SERVICE_ACCOUNT_LEXICON_7575C
# repo secret from GitHub → Settings → Secrets.

name: "[DISABLED] Firebase Hosting (superseded by Vercel)"

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Why are you manually triggering this disabled workflow?"
        required: true

jobs:
  noop:
    name: "This workflow is disabled"
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "thelexicon.xyz is now served by Vercel."
          echo "This Firebase workflow is retained for reference only."
          echo "Reason for manual trigger: ${{ github.event.inputs.reason }}"
